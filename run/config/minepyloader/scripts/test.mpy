#package test

public load void load():
    String a = "Hello World"
    int index = 0
    char nowChar

    while(index < String.lengthOf(a) ):
        nowChar = String.charAt(a, index)
        if(nowChar != ' '):
            Logger.log("text",nowChar)
        index = index + 1

    Logger.log("Test",c)

public ingame void test(String a):
    Logger.log("ingame", a)